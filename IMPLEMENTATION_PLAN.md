# office-lunch 実行計画書

## 概要

`office-lunch` は、お昼ご飯の感想・コスト・写真を日付ごとに記録する個人利用向けの React Web アプリケーションです。

カレンダービューから任意の日付を選択し、その日のランチ記録を追加・確認・編集できることを主目的とします。構成は最小限かつシンプルにし、まずはローカル環境で完結する実装を目指します。

## 目的

- 日々のランチ内容を気軽に振り返れるようにする
- 食事代を記録し、月間のランチ費用を概算できるようにする
- 写真・評価・感想を残すことで、再訪や節約の判断材料にする

## 対象ユーザー

- 個人利用者
- オフィス勤務や外出時のランチを記録したい人
- 複雑な家計簿ではなく、ランチに特化して簡単に管理したい人

## 機能要件

### カレンダービュー

- 月単位のカレンダーを表示する
- 任意の日付を押下できる
- 記録済みの日付は視覚的に判別できる
- 月の前後移動ができる
- 選択した日付のランチ記録を表示する

### 食事記録

- 選択した日付に対してランチ記録を作成できる
- 既存の記録を編集できる
- 記録内容を削除できる
- 記録項目は以下とする
  - 日付
  - カテゴリ
  - メニュー
  - 店
  - 食事代
  - 星5段階評価
  - 写真
  - 感想メモ

### カテゴリ

- 食事記録のカテゴリは以下の3種類とする
  - 外食
  - 購入
  - 自炊
- `外食` または `購入` を選択した場合のみ、店選択フォームを表示する
- 店は店テーブルから選択できる
- 未登録の店は記録時に新規追加できる
- `自炊` を選択した場合、店選択フォームは表示しない

### 店別集計

- 店ごとの利用回数を表示する
- 店ごとの平均評価を表示する
- 店ごとの平均食事代を表示する
- 集計対象は外食・購入で店を選択した記録とする

### 写真アップロード

- 食事記録時に写真をアップロードできる
- アップロードした写真をプレビュー表示できる
- 個人利用・ローカル完結を優先し、初期実装ではブラウザのローカルストレージまたは IndexedDB に保存する
- 写真サイズが大きくなりすぎないよう、保存前の圧縮またはリサイズを検討する

### 評価

- 星5段階で評価を登録できる
- 未評価状態も許容する
- 表示時は星アイコンで直感的に確認できるようにする

### 食事代

- 食事代を数値で記録できる
- 未入力も許容する
- 月間合計には、食事代が入力されている記録のみ含める

### 月間コスト概算

- 表示中の月に記録されたランチ代の合計を表示する
- 記録件数と平均単価も表示する
- 食事代未入力の記録は合計・平均から除外する

## 非機能要件

- 個人利用想定のため、バックエンドは初期実装では用意しない
- データ保存はブラウザ内に閉じる
- 画面構成はシンプルにし、1画面で主要操作が完結することを優先する
- スマートフォンでも最低限使えるレスポンシブ対応を行う
- 認証機能は実装しない
- 複数ユーザー対応は行わない

## 技術方針

### フロントエンド

- React
- TypeScript
- Vite

### スタイリング

- CSS Modules または通常の CSS
- UI ライブラリは導入しない、または最小限に留める
- アイコンが必要な場合は `lucide-react` の利用を検討する

### データ保存

初期実装では以下のいずれかを採用する。

1. `localStorage`
   - 実装が簡単
   - 画像保存には不向き
   - 小規模なテキストデータ中心なら十分

2. `IndexedDB`
   - 写真データを扱いやすい
   - 実装はやや複雑
   - 個人利用アプリとして長く使うならこちらが適する

写真アップロード要件があるため、実装時は `IndexedDB` を第一候補とする。

## 想定データモデル

```ts
type LunchCategory = 'eat_out' | 'takeout' | 'home';

type LunchRecord = {
  id: string;
  date: string; // YYYY-MM-DD
  category: LunchCategory;
  menuName?: string;
  shopId?: string;
  shopName?: string;
  cost?: number;
  rating?: number; // 1-5
  photo?: {
    id: string;
    mimeType: string;
    dataUrl: string;
  };
  memo?: string;
  createdAt: string;
  updatedAt: string;
};

type Shop = {
  id: string;
  name: string;
  createdAt: string;
  updatedAt: string;
};
```

## 画面構成

### メイン画面

- ヘッダー
  - アプリ名
  - 表示中の年月
- 月間サマリー
  - 月間ランチ代
  - 記録件数
  - 平均単価
- カレンダー
  - 日付一覧
  - 記録済みマーク
  - 選択日ハイライト
- 選択日の記録パネル
  - 記録がない場合は新規作成フォーム
  - 記録がある場合は詳細表示と編集導線

### 記録フォーム

- カテゴリ選択
- メニュー入力
- 店選択
- 新規店名入力
- 食事代入力
- 星評価
- 写真アップロード
- 感想メモ
- 保存ボタン
- 削除ボタン

## 実装ステップ

### 1. プロジェクト作成

- `office-lunch` 配下に Vite + React + TypeScript のプロジェクトを作成する
- 基本的な npm scripts を確認する
- 初期表示が動作することを確認する

### 2. アプリ骨格の作成

- メインレイアウトを作成する
- 表示中年月の状態を管理する
- 選択日付の状態を管理する

### 3. カレンダー実装

- 月の日付一覧を生成する
- 前月・翌月へ移動できるようにする
- 日付押下で選択日を切り替える
- 記録済み日付を判別して表示する

### 4. 記録フォーム実装

- カテゴリ選択を実装する
- カテゴリに応じて店選択フォームを出し分ける
- 店テーブルから既存店を選択できるようにする
- 未登録の店を新規追加できるようにする
- 食事代入力を実装する
- 星評価を実装する
- メモ入力を実装する
- 入力値の簡易バリデーションを行う

### 5. 写真アップロード実装

- ファイル選択を実装する
- 選択画像のプレビューを表示する
- 保存用データへ変換する
- 必要に応じて画像リサイズを行う

### 6. データ永続化

- IndexedDB の保存・取得・更新・削除処理を実装する
- アプリ起動時に保存済みデータを読み込む
- 保存後にカレンダーとサマリーへ反映する

### 7. 月間サマリー実装

- 表示中の月に該当する記録を集計する
- 月間合計金額を表示する
- 記録件数を表示する
- 平均単価を表示する

### 8. UI 調整

- 個人利用向けにシンプルで見やすい画面に整える
- PC とスマートフォンで表示崩れがないか確認する
- ボタン・フォーム・カレンダーの操作性を確認する

### 9. 動作確認

- 新規記録を作成できること
- 記録を編集できること
- 記録を削除できること
- 写真をアップロードし、再表示できること
- 外食・購入の場合のみ店選択フォームが表示されること
- 自炊の場合は店選択フォームが表示されないこと
- 店別の利用回数・平均評価・平均食事代が表示されること
- 月間ランチ代が正しく概算されること
- ブラウザを再読み込みしても記録が残ること

## 初期実装でやらないこと

- ユーザー認証
- クラウド同期
- 複数ユーザー共有
- レシート読み取り
- 地図連携
- 外部 API 連携
- 詳細な予算管理
- CSV エクスポート

## 将来的な拡張候補

- 月間予算の設定
- カテゴリ別集計
- 評価順・金額順の一覧表示
- CSV エクスポート
- 写真の圧縮品質設定
- データのバックアップ・復元
- PWA 対応

## 完了条件

- React アプリケーションとしてローカルで起動できる
- カレンダーから日付を選択できる
- ランチ記録を作成・編集・削除できる
- 写真を登録して表示できる
- 星5段階評価を登録できる
- 外食・購入の場合のみ店を選択または追加できる
- 店別の利用回数・平均評価・平均食事代を確認できる
- 食事代を記録できる
- 月間ランチ代の概算を確認できる
- 個人利用に十分なシンプルな UI になっている
